# 脚本 UI 重设计 · 交付总览

## 交付物

| 文件 | 说明 |
|---|---|
| `docs/ui-redesign/design-system.md` | 「糖果积木 Candy Blocks」设计系统：令牌（色彩/字体/形状/动效）、灵动岛与配置面板布局规格、组件库、可达性、ImGui 落地映射与 QA 清单 |
| `docs/ui-redesign/prototype.html` | 单文件高保真交互原型：1600×900 游戏场景 + 灵动岛 + 配置面板全流程，可直接浏览器打开体验 |

## 核心决策

- **视觉语言**：Neo-Brutalism「糖果积木」——3px 墨色描边 + 糖果色块 + 短位移硬阴影，替代现有 QQ2007 蓝渐变（面板）与 iOS 深色（灵动岛）两套割裂风格
- **字体**：中文阿里妈妈数黑体、数字/英文 Oxanium（用户既有规范）
- **行为零变更**：灵动岛 6s 自动收起、面板遮挡自动暂停/关闭恢复、启动后面板自动收起等已验证交互全部保留，仅重绘视觉
- **零迁移成本**：只改 `theme.go` 令牌与 `*_android.go` 绘制层，Task/Field 描述符与设备 `ui.json` 完全兼容
- **易用性强化**：触控目标全部 ≥48px；灵动岛展开按钮新增文字标签；任务行升级为卡片（大开关直接控制启用）；数字字段改为 ± 步进器；系统页增加危险区二次确认

## 后续建议

- 按 design-system.md §7 清单逐文件替换绘制层（theme.go → island → panel → taskpage → form → components）
- 阿里妈妈数黑体需打包进 APK 资源并在 ImGui 字体内嵌
- 步进器长按连发、任务状态区扩展可作为二期增强
