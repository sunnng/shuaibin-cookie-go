# 帅宾 Cookie

Android 端王国类游戏自动化：用户配置并启动脚本，由调度器轮询各任务完成刷取。

术语同时约束文档、讨论、UI 文案与代码标识符：新代码不得引入 _Avoid_ 词，存量违规标识符按 ADR-0001 的计划收敛。例外：标注为「实现细节 / 实现用词 / 实现」的 _Avoid_ 词可作机制名留在代码中（如 runtime、Store、Shell）；旧称与易混淆词（如 Module、Session）则全面禁绝。

## Language

**脚本 (Script)**：
用户点「运行」开启、可暂停/继续/停止的一次自动化生命周期。
_Avoid_: Session（易与竞技场进度、HTTP 会话混淆）, Runtime（实现细节）, Run

**任务 (Task)**：
脚本中可独立启用、调度与配置的一种游戏自动化能力（如王国竞技场）。
_Avoid_: 模块 (Module), BuiltinModule（面板列表项的旧称）

**任务状态 (Task State)**：
某任务的进度与运行快照（如已战斗场次、胜率、下次免费刷新）；灵动岛上的数字是它的 UI 展示。停止后再运行（新脚本）时，脚本内易失字段清零，跨脚本需保留的字段从持久化恢复。
_Avoid_: Session, arena.Session, 本轮统计（勿与任务状态并列成第二套概念）

**灵动岛 (Dynamic Island)**：
脚本运行时展示任务状态的悬浮胶囊 UI。
_Avoid_: HUD, 浮动球（打包选项 showFloatingBall 是另一套壳能力，勿与灵动岛混称）

**特征 (Feature)**：
某任务在基准分辨率下的 UI 常量集合（区域、色串、OCR 区等），只描述「是什么」，不含点击/导航行为。
_Avoid_: 把行为写进特征；与「比色特征」混称

**基准分辨率**：
特征常量（区域、点位、色串坐标）与 UI 布局常量（岛尺寸、面板栅格、字号）书写时采用的固定设计分辨率 1600×900；运行时按设备实际分辨率缩放换算。
_Avoid_: 设计分辨率, 屏幕分辨率（易与设备实际分辨率混淆）

**比色特征 (Color Feature)**：
一条用于多点/单点比色识别的色串 + 相似度；是特征表里的原子识别单元，对应实现里的 `screen.Feature`。
_Avoid_: 直接叫 Feature（会与任务级特征表撞名）

**页面 (Page)**：
基于特征判断「当前是否在此界面」、读取界面数据、以及页内点击/手势。
_Avoid_: 把跨页导航塞进页面

**路线 (Route)**：
跨多个页面的进出导航（例如从王国首页进入竞技场大厅，或离开大厅）。
_Avoid_: 路由（易联想到 HTTP/URL）

**弹窗 (Dialog)**：
游戏内浮在页面之上的提示/确认界面（如「缺少配料」「部署更多」）；不同名称的弹窗是不同的弹窗，其比色与确认区写在任务特征里。
_Avoid_: 把所有打断都叫弹窗

**陷阱 (Trap)**：
注册到 Guard 的一项可检测、可处理的打断（比色特征 + 处理动作 + 优先级）；弹窗常被注册为陷阱，但陷阱不限于弹窗。
_Avoid_: 与弹窗一一等同

**Guard**：
按优先级扫描已注册陷阱并处理首个命中项的机制；脚本主循环与任务状态机都会周期性调用。

**配置 (Config)**：
任务的用户选项（启用、战斗上限、奖杯差等）；面板编辑的就是配置；仓库/打包里的 `config.json` 是默认种子，不是设备上的唯一真源。
_Avoid_: 把任务状态持久化字段叫配置；Store（实现用词）

**面板偏好**：
配置面板自身的导航/选中等非业务 UI 状态（随 ui.json 落盘），不属于配置，也不属于任务状态。
_Avoid_: 配置, 任务状态

**步骤 (Step)**：
任务流程状态机中的当前节点（如 detect、navigate、battle）；回答「卡在哪」时指步骤，不指任务状态。
_Avoid_: 状态（裸用）, 阶段（若未约定）, 把步骤叫任务状态

**启用 (Enabled)**：
配置允许该任务参与调度；未启用则既不执行也不进入等待。
_Avoid_: 把冷却中或打满误称为未启用

**就绪 (Ready)**：
已启用的任务此刻可以执行一轮（冷却结束、未达上限等前置满足）。
_Avoid_: 与启用混用

**等待 (Waiting)**：
已启用但未就绪；带剩余时间与原因（如免费刷新等待）。
_Avoid_: 禁用

**调度器 (Scheduler)**：
脚本运行中按注册顺序轮询任务、执行当前就绪者的机制；任务的先后次序即注册次序，不另设优先级概念（除非日后显式引入）。
_Avoid_: Priority（当前）

**一轮调度**：
调度器一次扫表：按注册顺序对当前所有就绪任务各执行一遍。

**空闲 / 运行中 / 已暂停**：
脚本生命周期的三态：空闲=未在运行（开始前或停止后），运行中=执行中，已暂停=暂停中。**自动暂停**是已暂停的一种原因（如打开配置面板遮挡画面），不是第四态；与任务的启用/就绪无关。
_Avoid_: Session 态, 把任务未就绪叫已暂停

**配置面板 (Config Panel)**：
编辑配置与面板偏好、并控制脚本启停的主界面；与灵动岛并列，前者主编辑，后者精简操控与任务状态展示。
_Avoid_: 面板（裸用，易与游戏内界面混淆）, 控制台, Shell（实现）

**脚本 UI 框架 (UI Framework)**：
从本项目抽取、基于 ImGui 的可复用脚本 UI 层：灵动岛、配置面板、控件 kit、主题、缩放皆由其提供；应用仅以描述符与之接缝，框架不感知具体游戏与配置类型。见 ADR-0002。
_Avoid_: 把游戏知识放进框架, 框架 import 应用包（依赖方向只能 应用→框架）

**描述符 (Descriptor)**：
应用启动时显式构造并传给框架的声明式数据：任务描述符（ID/标题/分类/字段列表）、字段描述符、导航条目。框架据描述符驱动面板渲染与配置绑定；不是运行期全局注册表。
_Avoid_: 注册表 (Registry), Register 全局函数, 编译期写死在框架里的任务表

**字段 (Field)**：
一项配置的唯一声明处：键、类型、默认值、控件形态与读写绑定；种子写入、回写配置、面板渲染都由字段推导，取代「四处同键」。
_Avoid_: 在多处重复同一键的手工绑定

**主题 (Theme)**：
框架的视觉令牌集合（颜色、圆角、间距）；QQ 蓝为默认主题，应用可整套替换或覆盖个别令牌；不提供运行时切换。
_Avoid_: 多主题运行时切换（当前）

**组件 (Component)**：
框架 UI 的构造单元：每帧执行的函数，唯一输入是一个 Props 结构体（数据 + 回调），可自由组合嵌套；`Form` 即消费字段描述符的自动渲染组件。无保留控件树、无 diff。见 ADR-0003。
_Avoid_: 保留式组件树, VDOM, 类组件/继承, 控件对象句柄

**组件状态 (Component State)**：
框架托管的组件实例私有状态（如输入框草稿、折叠展开），以「组件嵌套路径 + 显式键」寻址；同一组件多处使用各自隔离。
_Avoid_: 调用位次 hooks（禁条件调用，脆）, 全局扁平键（撞键）
